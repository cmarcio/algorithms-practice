#include <iostream>

using namespace std;

int main() {
    cout << "INSTRUCTIONS\n";
    cout << "   *always call 'make' commands on the project root directory\n";
    cout << "   *call 'make dir=directory_name' to compile the files inside the desired directory\n";
    cout << "   *call 'make run' to run the program compiled\n";
    cout << "   *call 'make clean' to delete the object and program files\n";
    return 0;
}